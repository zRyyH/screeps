export const SPAWN_NAME = 'zRyyH BASE';
export const LIMITE_CREEPS = 20;
export const BODY_PADRAO: BodyPartConstant[] = [WORK, CARRY, MOVE];
export const BODY_BUILDER: BodyPartConstant[] = [WORK, WORK, CARRY, CARRY, MOVE, MOVE];

export const RATIO_BUILDERS = 0.3; // até 30% dos creeps serão builders quando houver obras
